<KnowledgeGraph VERSION="1.0.0">
  <M-CONFIG NAME="ConfigManager" TYPE="UTILITY">
    <file>src/config.py</file>
    <verification-ref>V-M-CONFIG</verification-ref>
    <annotations>
      <fn-load_config>Загружает конфигурацию из файла или переменных окружения.</fn-load_config>
    </annotations>
  </M-CONFIG>

  <M-DATASTORE NAME="DataStore" TYPE="DATA_LAYER">
    <file>src/datastore.py</file>
    <verification-ref>V-M-DATASTORE</verification-ref>
    <annotations>
      <fn-add_packet>Добавляет пакет в хранилище.</fn-add_packet>
      <fn-get_packets>Возвращает все пакеты за период.</fn-get_packets>
      <fn-clear>Очищает хранилище.</fn-clear>
    </annotations>
  </M-DATASTORE>

  <M-COLLECTOR NAME="PacketCollector" TYPE="CORE_LOGIC">
    <file>src/collector.py</file>
    <verification-ref>V-M-COLLECTOR</verification-ref>
    <dependencies>
      <CrossLink target="M-DATASTORE"/>
    </dependencies>
    <annotations>
      <fn-handle_packet>Обрабатывает входящий пакет.</fn-handle_packet>
      <fn-start>Запускает сборщик пакетов.</fn-start>
      <fn-stop>Останавливает сборщик пакетов.</fn-stop>
      <fn-is_running>Проверяет, запущен ли сборщик.</fn-is_running>
    </annotations>
  </M-COLLECTOR>

  <M-STATS NAME="StatsCalculator" TYPE="CORE_LOGIC">
    <file>src/stats.py</file>
    <verification-ref>V-M-STATS</verification-ref>
    <dependencies>
      <CrossLink target="M-DATASTORE"/>
    </dependencies>
    <annotations>
      <fn-get_total_messages>Подсчитывает общее количество сообщений.</fn-get_total_messages>
      <fn-get_top_senders>Определяет N самых активных участников.</fn-get_top_senders>
      <fn-get_top_heard_nodes>Определяет N самых "слышимых" узлов.</fn-get_top_heard_nodes>
      <fn-get_portnum_distribution>Рассчитывает распределение типов сообщений.</fn-get_portnum_distribution>
      <fn-get_all_stats>Возвращает всю статистику в одном отчете.</fn-get_all_stats>
    </annotations>
  </M-STATS>

  <M-WEATHER NAME="WeatherService" TYPE="INTEGRATION">
    <file>src/weather.py</file>
    <verification-ref>V-M-WEATHER</verification-ref>
    <dependencies>
      <CrossLink target="M-CONFIG"/>
    </dependencies>
    <annotations>
      <fn-__init__>Инициализирует сервис погоды.</fn-__init__>
      <fn-get_weather>Получает данные о погоде для указанной локации.</fn-get_weather>
      <fn-_fetch_weather_data>Выполняет запрос к API погоды (mock-реализация).</fn-_fetch_weather_data>
      <fn-get_last_weather>Возвращает последние полученные данные о погоде.</fn-get_last_weather>
    </annotations>
  </M-WEATHER>

  <M-REPORTER NAME="ReportGenerator" TYPE="CORE_LOGIC">
    <file>src/reporter.py</file>
    <verification-ref>V-M-REPORTER</verification-ref>
    <dependencies>
      <CrossLink target="M-STATS"/>
      <CrossLink target="M-WEATHER"/>
    </dependencies>
    <annotations>
      <fn-__init__>Инициализирует генератор отчетов.</fn-__init__>
      <fn-generate_report>Генерирует отчет за период.</fn-generate_report>
      <fn-_build_summary>Создает текстовое резюме отчета.</fn-_build_summary>
      <fn-format_report_text>Форматирует отчет в текстовом виде.</fn-format_report_text>
    </annotations>
  </M-REPORTER>

  <M-MESHTASTIC NAME="MeshtasticInterface" TYPE="INTEGRATION">
    <file>src/meshtastic_interface.py</file>
    <verification-ref>V-M-MESHTASTIC</verification-ref>
    <dependencies>
      <CrossLink target="M-CONFIG"/>
    </dependencies>
    <annotations>
      <fn-__init__>Инициализирует интерфейс Meshtastic.</fn-__init__>
      <fn-connect>Подключается к сети Meshtastic.</fn-connect>
      <fn-disconnect>Отключается от сети Meshtastic.</fn-disconnect>
      <fn-send_message>Отправляет сообщение в сеть Meshtastic.</fn-send_message>
      <fn-_send_mock_message>Mock-реализация отправки сообщения.</fn-_send_mock_message>
      <fn-is_connected>Проверяет, подключен ли интерфейс.</fn-is_connected>
      <fn-get_sent_messages>Возвращает список отправленных сообщений.</fn-get_sent_messages>
    </annotations>
  </M-MESHTASTIC>

  <M-SCHEDULER NAME="Scheduler" TYPE="ENTRY_POINT">
    <file>src/main.py</file>
    <verification-ref>V-M-SCHEDULER</verification-ref>
    <dependencies>
      <CrossLink target="M-COLLECTOR"/>
      <CrossLink target="M-REPORTER"/>
      <CrossLink target="M-MESHTASTIC"/>
    </dependencies>
    <annotations>
      <fn-__init__>Инициализирует планировщик.</fn-__init__>
      <fn-schedule_daily_report>Планирует ежедневную генерацию отчета.</fn-schedule_daily_report>
      <fn-start>Запускает планировщик.</fn-start>
      <fn-stop>Останавливает планировщик.</fn-stop>
      <fn-is_running>Проверяет, запущен ли планировщик.</fn-is_running>
      <fn-run_daily_report_job>Выполняет задачу генерации и отправки ежедневного отчета.</fn-run_daily_report_job>
      <fn-get_scheduled_tasks>Возвращает список запланированных задач.</fn-get_scheduled_tasks>
      <fn-run_async>Асинхронно запускает планировщик с выполнением задач по расписанию.</fn-run_async>
      <fn-_check_and_run_tasks>Проверяет и выполняет задачи по расписанию.</fn-_check_and_run_tasks>
      <fn-main>Точка входа приложения.</fn-main>
    </annotations>
  </M-SCHEDULER>
</KnowledgeGraph>
